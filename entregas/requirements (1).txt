# ChargeGrid Intelligence — EV Challenge 2026 — GoodWe / FIAP
#
# Como instalar:
#   pip install -r requirements.txt
#
# ATENÇÃO: o Ollama NÃO é instalado por este arquivo.
# No Colab, a Célula 1 do notebook já cuida disso automaticamente.
# Para rodar local: curl -fsSL https://ollama.com/install.sh | sh

# Necessário apenas se USAR_API_REAL_MERCADOPAGO = True
requests>=2.28.0

# IA Preditiva — carregar e usar o modelo_demanda.pkl
scikit-learn>=1.1.0
joblib>=1.2.0

# Chatbot RAG — client Python do Ollama + interface do notebook
ollama>=0.1.8
ipywidgets>=8.0.0

# Dashboard Streamlit
streamlit>=1.28.0

# Pipeline de análise — necessário apenas para regenerar dados_rag.json a partir
# da planilha SP2 (Trabalho_Analise_Comercial_SP2.xlsx) ou o notebook de treino do modelo.
# Não é exigido para rodar o sistema, o chatbot ou o dashboard.
pandas>=1.5.0
matplotlib>=3.6.0
openpyxl>=3.0.10
